<!DOCTYPE html>
<html ng-app="Advertiser">
<% include header %>
<body>
	<% include navbar %>

	<!-- Main jumbotron for a primary marketing message or call to action -->
	<div class="jumbotron">
		<div class="container">
			<h1>Advertisers</h1>
			<div ng-controller="AdvertiserController">
                <h3> All existing advertisers </h3>

		<select>
        <% result.forEach(function(res) { %>
        <option value = \" <%= res.ad_addr %> \"> <%= res.ad_addr %></option>
        <% }); %>
</select>
                <!--
				<form ng-submit="addadvertiser()">
					<div class="col-lg-6">
						<div class="input-group input-group-lg">
							<input class="form-control" type="text" ng-model="advertiserText" size="30" placeholder="add new advertiser here"> 
							<input class="form-control" type="text" ng-model="aggregatorText" size="30" placeholder="add new aggregator here"> 
							<input class="form-control" type="text" ng-model="updatetimeText" size="30" placeholder="add new updatetime here"> 
							<input class="form-control" type="text" ng-model="rssiText" size="30" placeholder="add new rssi here"> 
							
                                <span
								class="input-group-btn">
								<button class="btn btn-primary btn-lg" type="submit">Add</button>
							</span>
						</div>
					</div>
				</form>
-->
			</div>
		</div>
	</div>

	<div class="container">
		<hr>

		<% include footer %>
	</div>
	<!-- /container -->
	<script type="text/javascript">
		$(document).ready(function() {
			$('#home').addClass("active");
		});
	</script>
</body>
</html>
